import type { StyleMode, ThemeMode, WeekNumber } from "@/lib/course/types";

export const DEFAULT_THEME_MODE: ThemeMode = "light";
export const DEFAULT_STYLE_MODE: StyleMode = "bento-grid";
export const DEFAULT_WEEK: WeekNumber = 1;
